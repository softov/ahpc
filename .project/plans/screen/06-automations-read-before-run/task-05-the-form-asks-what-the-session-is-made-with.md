---
title: The form asks what the run's session is made with
status: done
depends: []
layer: "screen"
refs:
  - "[code://Harness and Model selects from controller.agents(); the host's session questions from controller.resolveSettings() (resolveSessionConfig, re-asked as answers and the directory change, the directory debounced) and the model's configSchema, as Selects laid out up to three to a row; a dynamic question is typed; Missed (runOnce or skip) beside the preset. The label column is sized to the longest first-column label so every field starts in one column, and the prompt's inset follows the theme's input border.](../../../../Harness and Model selects from controller.agents(); the host's session questions from controller.resolveSettings() (resolveSessionConfig, re-asked as answers and the directory change, the directory debounced) and the model's configSchema, as Selects laid out up to three to a row; a dynamic question is typed; Missed (runOnce or skip) beside the preset. The label column is sized to the longest first-column label so every field starts in one column, and the prompt's inset follows the theme's input border.) - where it changed"
---

## Objective

test/automations.test.tsx: the form at 60 and 90 columns (24 rows paper, 36 rows dark), the harness, model and host settings are offered, and the prompt lines up with the name in paper and dark.

## Validation

- 
- `npx vitest run` passes.

## Resume

Done 2026-09-26, validated by Softov.
